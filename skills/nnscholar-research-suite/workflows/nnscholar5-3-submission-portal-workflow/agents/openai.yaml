interface:
  display_name: "NNScholar 5.3 Submission Portal Workflow"
  short_description: "Prepare portal fields, uploads, and safety checks."
  default_prompt: "Use $nnscholar5-3-submission-portal-workflow to prepare submission portal field answers, upload manifest, safety-gated steps, and final human confirmation checklist."
policy:
  allow_implicit_invocation: true
